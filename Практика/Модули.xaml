<Window x:Class="Практика.Настройки"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Практика"
        mc:Ignorable="d"
        Title="Настройки" Height="450" Width="800">
    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="/x2hLjgymx0o.jpg"/>
        </Grid.Background>
        <Button Content="В главное меню" HorizontalAlignment="Left" Margin="10,384,0,0" VerticalAlignment="Top" Height="30" Click="Button_Click" Background="{x:Null}" BorderBrush="{x:Null}" Foreground="White" FontSize="16" FontWeight="Bold" ToolTip="В главное меню" Cursor="Hand"/>
        <Button Content="Плеер" HorizontalAlignment="Left" Height="40" Margin="60,149,0,0" VerticalAlignment="Top" Width="85" Cursor="Hand" Background="{x:Null}" BorderBrush="{x:Null}" Foreground="White" FontSize="20" FontWeight="Bold" FontStyle="Italic" ToolTip="Плеер" Click="Button_Click_1"/>

    </Grid>
</Window>
